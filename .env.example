# Environment configuration template for the CRM-LRM backend.
# Copy this file to `.env` and replace the placeholder values as needed.

# Primary connection string used by Prisma Client.
# Neon deployments behind PgBouncer require the pgbouncer and sslmode options.
DATABASE_URL="postgresql://USER:PASSWORD@YOUR-NEON-HOST/db-name?sslmode=require&pgbouncer=true&connect_timeout=10"

# Direct connection string used for migrations and long-running queries.
# This should point to the same database but bypass PgBouncer.
DIRECT_URL="postgresql://USER:PASSWORD@YOUR-NEON-HOST/db-name?sslmode=require&connect_timeout=10"

# Runtime environment (development | test | production).
NODE_ENV=development

# Secret used to sign JSON Web Tokens.
JWT_SECRET="replace-with-a-strong-secret"

# Optional settings ---------------------------------------------------------

# TCP port for the HTTP server (defaults to 3001 when omitted).
#PORT=3001

# Allowed origins for CORS requests. Provide a comma-separated list if needed.
#CORS_ORIGIN="http://localhost:3000"

# Rate limiting configuration (defaults shown).
#RATE_LIMIT_WINDOW_MS=900000
#RATE_LIMIT_MAX_REQUESTS=100
